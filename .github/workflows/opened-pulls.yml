name: Move open pull requests into In Progress

on:
  pull_request:
    types: [opened]

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: edwingl10/portfoliosite@v0.0.0
        with:
          project: Portfolio Website
          column: In Progress
          repo-token: ${{ secrets.GITHUB_TOKEN }}
